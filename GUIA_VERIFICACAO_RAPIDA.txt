╔═══════════════════════════════════════════════════════════════════════════╗
║           GUIA RÁPIDO: VERIFICAR USUÁRIOS NO PostgreSQL                   ║
╚═══════════════════════════════════════════════════════════════════════════╝

📌 SITUAÇÃO ATUAL:
   • credentials.json: contém apenas "marco"
   • config.yaml: contém apenas "marco"
   • PostgreSQL: ?? (precisa verificar)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 VERIFICAÇÃO AGORA (Execute no terminal Coolify):

   cd /app && python verificar_usuarios_postgres.py


💡 O QUE VOCÊ VERÁ:

   Opção 1 - 4 usuários no banco:
   ═══════════════════════════════════════════════════════════════════
   📊 VERIFICAÇÃO: Usuários no PostgreSQL
   ═══════════════════════════════════════════════════════════════════
   Total de usuários no banco: 4

   Username             Email                          Status       Criado em
   ──────────────────────────────────────────────────────────────────────────
   marco                marco@sensebike.com.br        Ativo        2025-11-26
   kennedy.oliveira     kennedy@sensebike.com.br      Ativo        2025-11-27
   alisson.galvao       alisson@sensebike.com.br      Ativo        2025-11-27
   marcos.fernandes     marcos@sensebike.com.br       Ativo        2025-11-28


   Opção 2 - Apenas 1 usuário no banco:
   ═══════════════════════════════════════════════════════════════════
   📊 VERIFICAÇÃO: Usuários no PostgreSQL
   ═══════════════════════════════════════════════════════════════════
   Total de usuários no banco: 1

   Username             Email                          Status       Criado em
   ──────────────────────────────────────────────────────────────────────────
   marco                marco@sensebike.com.br        Ativo        2025-11-26


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 PRÓXIMO PASSO (após verificação):

   1️⃣  Se aparecerem 4 usuários:
       ✅ Dados estão salvos no PostgreSQL!
       → Vamos recuperar os 3 usuários perdidos
       → Atualizar credentials.json
       → Implementar sincronização com BD

   2️⃣  Se aparecer apenas "marco":
       ⚠️  Os 3 usuários foram perdidos
       → Infelizmente não há recuperação
       → Você precisará criá-los novamente
       → Mas implementaremos proteção para evitar isso no futuro

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🛠️ OUTROS COMANDOS ÚTEIS:

   # Ver conteúdo de credentials.json (o que existe localmente)
   cat credentials.json

   # Ver conteúdo de config.yaml (o que existe localmente)
   cat config.yaml

   # Ver quantos usuários existem (sem detalhes)
   python -c "import psycopg2; conn = psycopg2.connect(host='u48cw44ccwg4sowco4044goc', user='postgres', password='poMaf572450+@', database='postgres'); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM usuarios'); print(f\"Total: {cur.fetchone()[0]} usuários\"); conn.close()"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏰ PRÓXIMO:

   Compartilhe o resultado com a seguinte informação:
   • Quantos usuários aparecerem no PostgreSQL
   • Nomes de cada um (se possível, copie a tabela)
   • Qualquer erro que receba

   Baseado nisso, diremos como recuperar os dados ou implementar proteção.

╔═══════════════════════════════════════════════════════════════════════════╗
║         Execute agora: cd /app && python verificar_usuarios_postgres.py    ║
╚═══════════════════════════════════════════════════════════════════════════╝
